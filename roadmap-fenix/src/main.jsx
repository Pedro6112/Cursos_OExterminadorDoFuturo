import React from "react";
import ReactDOM from "react-dom/client";
import CourseRoadmap from "./CourseRoadmap.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CourseRoadmap />
  </React.StrictMode>
);
